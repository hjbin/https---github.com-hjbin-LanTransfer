文件传输的部分大概已经完成，实现以下功能
1，多线程传输，传输大文件速度比单线程快很多，多次实验，没有问题
2，支持断点续传，几种情况：a,接收端主动断开连接(设置里面的fileInterrupt值)  b,发送端主动断开(设置里面的fileInterrupt值) 
                            c,发送端程序崩溃     记录位置需要存到数据库里，取的时候也需要从数据库里面读，在代码里有TODO标记
3，理论上可以实现一对多传输，没有试验过
4，记录了已经传输的大小（TODO标记好了），用它除以文件大小，可以得到传输进度
5，用了AES加密防窃取，用了MD5校验防篡改